[package]
name = "grpc-dsl"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = { version = "0.14" }
tonic-prost = "0.14"
prost = { version = "0.14" }
prost-types = { version = "0.14" }
serde = { version = "1.0", features = ["derive"] }
pbjson = "0.9"          # 👈 新增
pbjson-types = "0.9"

[build-dependencies]
tonic-prost-build = "0.14"   # 👈 新增
